# Phase 13: 正しいモデル再構築 - 完全実装計画

## 目標
**未来データで実用に耐える、科学的に正しい予測システムの構築**

---

## ✅ 完了済み

### Step 1: データ分割（リーケージ排除）
- [x] データベースから2020-2024年データ抽出（289,336レース）
- [x] 時系列分割実行
  - 訓練: 2020-2022年（143,323レース）
  - 検証: 2023年（47,672レース）
  - テスト: 2024年（47,181レース）
- [x] 日付重複なし確認

**ファイル**:
- `phase13_train_2020_2022.csv`
- `phase13_val_2023.csv`
- `phase13_test_2024.csv`

---

## 🔄 次のステップ

### Step 2: 特徴量エンジニアリング（リーケージ排除版）

**作業内容**:
1. 既存の特徴量を監査
   - datetime.now()使用箇所を特定
   - 未来データ参照の可能性がある特徴量を洗い出し

2. リーケージなし特徴量の作成
   ```python
   # NG例
   horse_recent_5 = get_recent_races(horse_id, n=5)  # 現在時点から5走

   # OK例
   horse_recent_5 = get_recent_races(horse_id, n=5, cutoff_date=race_date)
   ```

3. 新規特徴量の追加（オプション）
   - 騎手・調教師の成績（過去データのみ）
   - コース適性（過去データのみ）
   - ペース指標（過去データのみ）

**期間**: 2-3日

**ファイル**: `phase13_feature_engineering.py`

---

### Step 3: モデル訓練（複数アルゴリズム比較）

**候補モデル**:
1. LightGBM（現行）
2. XGBoost
3. CatBoost
4. ロジスティック回帰（ベースライン）

**訓練手順**:
```
1. 訓練データ（2020-2022）でモデル訓練
2. 検証データ（2023）でハイパーパラメータ調整
3. 最良モデル選択
4. テストデータ（2024）で最終評価
```

**評価指標**:
- ◎本命的中率
- 3着以内的中率
- 回収率（シミュレーション）
- Calibration（予測確率 vs 実際の的中率）

**期間**: 2-3日

**ファイル**: `phase13_train_model.py`

---

### Step 4: Calibration（確率較正）

**問題**: 現在のモデルは「勝率50%」と予測しても実際は20%しか当たらない

**対策**:
1. Platt Scaling
2. Isotonic Regression
3. Temperature Scaling

**実装**:
```python
from sklearn.calibration import CalibratedClassifierCV

calibrated_model = CalibratedClassifierCV(
    base_model, method='isotonic', cv='prefit'
)
calibrated_model.fit(X_val, y_val)  # 検証データで較正
```

**期間**: 1日

**ファイル**: `phase13_calibration.py`

---

### Step 5: Walk-Forward Validation（追加検証）

**目的**: 時系列データでの真の汎化性能を確認

**手順**:
```
訓練: 2020年 → テスト: 2021年
訓練: 2020-2021年 → テスト: 2022年
訓練: 2020-2022年 → テスト: 2023年
訓練: 2020-2023年 → テスト: 2024年
```

**期間**: 1-2日

**ファイル**: `phase13_walk_forward.py`

---

### Step 6: 2024年テスト＋2026年2月検証

**最終評価**:
1. 2024年（テストデータ）で性能評価
2. 2026年2月（完全な未来）で実戦検証

**合格基準**:
- 2024年本命的中率: ≥ 25%
- 2026年2月本命的中率: ≥ 20%（現状と同等以上）
- 回収率: ≥ 80%（許容範囲）

**期間**: 1日

**ファイル**: `phase13_final_evaluation.py`

---

### Step 7: GUI統合＋運用

**修正内容**:
1. `predict_core()`の完全書き換え
   - datetime.now()完全排除
   - current_dateを必須パラメータ化

2. 新モデルの組み込み
   - `model_phase13_win.pkl`
   - `model_phase13_top3.pkl`
   - `calibrator_phase13.pkl`（較正器）

3. 表示の修正
   - 予測確率を較正済み確率に
   - 過去の的中率表示（実績ベース）

**期間**: 1-2日

**ファイル**:
- `keiba_prediction_gui_v4.py`（新バージョン）
- `phase13_integration.py`

---

## 📊 予想される結果

### 保守的予測
- 2024年本命的中率: 22-25%
- 2026年2月本命的中率: 18-22%
- 回収率: 60-75%

### 理由
- リーケージ排除により性能低下は避けられない
- ただし過学習が減るため、未来データでの安定性は向上
- Calibrationにより予測確率の信頼性が向上

---

## 🎯 成功の定義

**必須条件**:
1. リーケージ完全排除の確認
2. 2024年と2026年2月の性能差が±5%以内（安定性）
3. 予測確率と実際の的中率のズレが10%以内

**理想条件**:
1. 本命的中率 ≥ 25%
2. 回収率 ≥ 80%
3. 日によるバラつきが現状より小さい

---

## ⏱️ 全体スケジュール

| ステップ | 期間 | 担当 |
|---------|-----|------|
| ~~Step 1: データ分割~~ | ~~完了~~ | ✅ |
| Step 2: 特徴量 | 2-3日 | 次 |
| Step 3: モデル訓練 | 2-3日 | |
| Step 4: Calibration | 1日 | |
| Step 5: Walk-Forward | 1-2日 | |
| Step 6: 最終評価 | 1日 | |
| Step 7: GUI統合 | 1-2日 | |
| **合計** | **9-14日** | |

---

## 📝 次に実行すべきコマンド

```bash
# Step 2開始
py phase13_feature_engineering.py
```

---

作成日: 2026-02-18
Phase: 13 (Rebuild from Zero)
Status: Step 1 完了 → Step 2 準備中
