# 引継ぎ指示書 — 2026年2月25日

**作成目的**: PC再起動後に Claude Code を立ち上げてすぐ作業を再開するための引継ぎ書。
**作業フォルダ**: `C:\Users\bu158\Keiba_Shisaku20250928`

---

## 再起動直後にやること（最優先）

### 1. SHAP表示の動作確認

今日の作業でGUIにSHAP予測根拠表示を実装した。
まず GUI を起動して動作確認する。

```bat
競馬予想ツール.bat
```

確認ポイント:
- ログ欄に `SHAP explainer 初期化完了` と表示されるか
- 任意のレースを読み込んで予測実行後、右下の推奨馬券テキストエリアの末尾に以下が表示されるか:

```
【予測根拠（単勝モデル TOP5特徴量）】
  ▶ 馬名 (XX.X%)
      ↑ 芝勝率          +0.037
      ↑ 騎手勝率        +0.017
      ↓ 前走着順        -0.012
      ...
```

- 問題なければ **SHAP実装完了** として今週末から使える状態

---

## 今日（2026/02/25）完了したこと

| 作業 | 内容 |
|---|---|
| タスクスケジューラ設定 | keiba_schedule_fetch / keiba_odds_morning / keiba_odds_afternoon の3タスクを登録（土日自動実行） |
| スリープ解除設定 | WakeToRun = true を3タスクに設定済み |
| SHAP実装 | `keiba_prediction_gui_v3.py` に予測根拠TOP5表示を追加 |

---

## 現在の全体状態

```
Phase 14 ✅ → Phase A/B/C ✅ → GUI統合 ✅ → NaT修正 ✅ → 運用ツール整備 ✅
→ タスクスケジューラ設定 ✅ → SHAP実装 ✅
                                              ↓
                                    今週末: ペーパートレード開始
```

---

## 完成しているファイル一覧

| ファイル | 内容 |
|---|---|
| `phase14_model_win.txt` | 単勝予測モデル（AUC 0.7988） |
| `phase14_model_place.txt` | 複勝予測モデル（AUC 0.7558） |
| `phase14_feature_list.pkl` | 39特徴量リスト |
| `keiba_prediction_gui_v3.py` | GUI本体（SHAP表示付き） |
| `paper_trade_log.csv` | ペーパートレード記録用CSV |
| `paper_trade_add.py` | ペーパートレード記録CLI（Kelly計算付き） |
| `paper_trade_review.py` | 月次レビュースクリプト（PSI・GO/NO-GO・Kelly比較） |
| `odds_collector/schedule_fetch.py` | レーススケジュール取得（土日07:00自動） |
| `odds_collector/odds_snapshot.py` | オッズスナップショット取得 |
| `odds_collector/odds_timeseries.db` | SQLiteデータベース（初期化済み） |
| `START_HERE.md` | プロジェクト概要・今週末の運用手順 |

---

## 今週末の運用手順（ペーパートレード開始）

タスクスケジューラが自動実行するが、手動でも可。

```bash
# 土日朝 07:00（自動 or 手動）
py odds_collector/schedule_fetch.py

# 各レース発走30分前（自動 or 手動）
py odds_collector/odds_snapshot.py --timing 30min_before

# GUI で予測
競馬予想ツール.bat
```

**Rule4 ベット条件**:
- `pred_win > 20%` かつ `2.0x ≤ オッズ < 10.0x`
- または `pred_win > 10%` かつ `オッズ ≥ 10.0x`

```bash
# レース後に記録
py paper_trade_add.py
```

---

## 今後のロードマップ

| 時期 | タスク |
|---|---|
| **今週末** | **ペーパートレード開始（上記手順）** |
| **再起動直後** | **SHAP動作確認（上記参照）** |
| 1〜2ヶ月後 | `py paper_trade_review.py` で GO/NO-GO 判定（200件・8週間目安） |
| 3〜6ヶ月後 | Phase D-1 オッズドリフト特徴量エンジニアリング |
| 3〜6ヶ月後 | Isotonic Regression キャリブレーション |
| 長期 | Phase D-2 月次モデル再訓練サイクル |

---

## Claude Code 起動時の確認事項

ClaudeCodeを立ち上げたら最初に以下を伝えるとスムーズ:

```
C:\Users\bu158\Keiba_Shisaku20250928\HANDOFF_20260225.md
このファイルを参照して作業の続きを進めて
```

---

## SHAP実装の詳細（トラブル時の参考）

### 変更したファイル
`keiba_prediction_gui_v3.py` の7箇所

### 変更内容
```python
# 1. import追加（L69付近）
try:
    import shap
    SHAP_AVAILABLE = True
except ImportError:
    SHAP_AVAILABLE = False

# 2. FEATURE_NAMES_JP 辞書（39特徴量の日本語名マッピング）

# 3. __init__ に追加
self.shap_explainer_win = None

# 4. load_models() に追加
if SHAP_AVAILABLE:
    self.shap_explainer_win = shap.TreeExplainer(self.model_win)

# 5. predict_core() に追加（pred_top3_proba の直後）
shap_top5 = []
if SHAP_AVAILABLE and self.shap_explainer_win is not None:
    try:
        sv = self.shap_explainer_win.shap_values(feat_df)[0]
        pairs = sorted(zip(feat_df.columns.tolist(), sv),
                       key=lambda x: abs(x[1]), reverse=True)[:5]
        shap_top5 = pairs
    except Exception:
        shap_top5 = []

# 6. predictions.append({}) に追加
'shap_top5': shap_top5,

# 7. update_recommended_bets() の末尾（テキスト挿入直前）に追加
# ◎・○馬のSHAP TOP5を表示するブロック
```

### エラー対応
- `SHAP explainer 初期化失敗` → `py -m pip install shap` を実行
- SHAP表示が出ない → `SHAP_AVAILABLE` が False になっている可能性（shap 未インストール）

---

## タスクスケジューラ確認コマンド

```powershell
Get-ScheduledTask | Where-Object {$_.TaskName -like "keiba_*"} | Select-Object TaskName, State
```

3タスクとも `Ready` なら正常。

---

*作成: 2026年2月25日*
